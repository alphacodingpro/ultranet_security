name: Deploy Website via FTP

on:
  push:
    branches:
      - main  # Agar branch master hai to master likhein

jobs:
  web-deploy:
    name: Deploying to cPanel
    runs-on: ubuntu-latest
    
    steps:
    - name: Get latest code
      uses: actions/checkout@v3
    
    - name: PHP syntax gate
      run: find . -name '*.php' -not -path './.git/*' -print0 | xargs -0 -n1 php -l

    - name: Build smaller copies of live homepage photos
      env:
        FTP_SERVER: ${{ secrets.FTP_SERVER }}
        FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
        FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
      run: |
        python3 -m pip install 'Pillow==11.3.0'
        python3 tools/build-web-images.py --live

    - name: Sync files to Server
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        server-dir: ./
        # Niche wali lines aapki files aur folders ko cPanel par update hone se bachayengi:
        exclude: |
          .env
          **/uploads/products**
          **/img/**
          **/assets/img/**
          **/.git*
          **/.git*/**
          **/tests/**
          **/tools/**
          **/node_modules/**
          **/.DS_Store
          seo-live-audit.json

    - name: Preserve hosting rules and redirect the legacy packages page
      env:
        FTP_SERVER: ${{ secrets.FTP_SERVER }}
        FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
        FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
      run: python3 tools/install-seo-redirects.py

    - name: Live SEO crawl after deployment
      run: python3 tools/seo-live-audit.py
    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: live-seo-audit
        path: seo-live-audit.json
