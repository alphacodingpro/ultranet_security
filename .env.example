# Copy these values into the live cPanel .env file. Never commit the real token.
APIFY_API_TOKEN=apify_api_replace_with_real_token
# Optional: only change this when you deliberately use a different Apify search actor.
APIFY_SEARCH_ACTOR=apify/google-search-scraper
